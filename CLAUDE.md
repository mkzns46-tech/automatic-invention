# CLAUDE.md --- ARICO ARCHERY Internal App

このファイルは、ARICO ARCHERY 社内アプリを Claude Code / Codex 等のコーディングエージェントが安全に変更するための恒久ルールです。

## 1. アプリの目的

ARICO ARCHERY の店舗・通販・イベント業務を1つの社内ポータルで管理する業務アプリです。

主な画面・業務：

- 在庫管理：在庫変動登録、アプリ内棚卸、スマレジ在庫変動チェック、棚卸分析、商品別履歴、全体履歴、商品転用、棚番登録
- バックオーダー / 取り寄せ管理
- イベント / ブース管理：イベント作成、持ち出し、持ち出し在庫一覧、CSV、ガチャ、販売取込、戻り、イベント終了、削除 / アーカイブ
- 販売管理：見積、請求、入金確認、納品、領収、販売進捗
- 出荷・納品・検品関連
- 商品マスター / 各種設定
- スマレジ連携

実店舗の実在庫・売上・イベント在庫を扱う本番業務システムである。
「画面が動く」だけで完了とせず、UI・DB・履歴・外部連携の整合性まで確認する。

## 2. 技術構成

- Frontend：素の HTML / CSS / JavaScript
- Hosting / Deploy：Vercel
- Server/API：Vercel `api/`
- Database：Supabase
- Source control：GitHub
- Repository：`mkzns46-tech/automatic-invention`
- External integration：スマレジ API
- その他：MakeShop 等

React 等のフレームワーク前提で作り直さない。既存の素のHTML/JS構成を尊重する。

過去に巨大な `app.js` 一本化を避ける方針を採用している。機能別ファイル分割を維持し、新規処理を安易に共通ファイルへ詰め込まない。

UIは在庫管理UIを基本とし、メニューバー、通知POP、通知音、余白、ボタン、カード、スマホ対応を同一アプリとして統一する。`alert()` は原則使わず既存の共通通知UIを使う。

## 3. デプロイ

GitHub の `main` が Vercel Production と接続されている。

原則：

1. 修正
2. 構文 / 静的チェック
3. 必要なローカルテスト
4. commit
5. `main` へ push
6. Vercel Production 反映確認
7. Production 実操作
8. DB / API / 履歴を突合
9. FAILなら修正して再deploy・再テスト

`main` への push は実質的に即本番デプロイにつながるものとして扱う。

Production確認では `version.js` の表示だけを信用せず、実際に配信されている対象JS assetも最新版であることを確認する。

## 4. Productionを触るルール

- Production既存データを理由なくDELETEしない。
- テスト前に対象商品の現在値を記録する。
- テスト後は変更した在庫・設定を原則元へ戻す。
- テストイベントは正式な削除 / アーカイブフローを使う。
- 履歴保護仕様を迂回して強制DELETEしない。
- 実顧客の販売・請求・納品データをテストに使わない。
- 実売上をテスト目的で無断登録しない。
- スマレジ書き込みテスト前に影響範囲を確認する。
- 存在しない `register_id` / `terminal_id` を捏造して外部APIへ送らない。
- 仮設定をProductionに残さない。
- 数量変更テストでは Before / After / 復元後を記録する。
- 複数更新の部分成功を避け、必要ならtransaction / RPCを使う。
- FAILは報告して終了せず、安全に修正可能なら再テストまで進める。

ログイン、MFA、権限承認、外部サービス側で本人操作が必要な場合のみユーザーへ確認する。

## 5. 商品識別子

Productionでは `barcode` を内部の商品識別子として広く使用する。

- 内部商品識別：`barcode`
- スマレジ商品ID：`products.smaregi_product_id`

存在を前提にしない / 使用禁止：

- `products.id`
- `product.id`
- `products.product_code`

商品コード入力は `smaregi_product_id` から商品を解決し、内部処理では必要に応じて `barcode` へ正規化する。

### barcode変更

スマレジ商品マスター取込で `smaregi_product_id` が同一でも `barcode` が変更されるケースがある。

`products.barcode` だけを単純UPDATEしない。barcodeを参照する在庫、棚番、棚卸、イベント、履歴、販売等を監査し、孤立データを発生させない。新barcodeが別商品に存在する等の衝突時は自動変更しない。

## 6. 在庫のSingle Source of Truth

### 通常在庫

通常棚の正式在庫は現行実装の `products.base_stock` を基準とする。

アプリ内棚卸は増減数ではなく現物実数を登録する。

- 現在10、棚卸8 → 正式在庫8
- 現在8、棚卸12 → 正式在庫12
- 現在12、棚卸12 → 在庫変更なし
- 0への棚卸も有効

棚卸確定は即時反映。旧仕様の「後で在庫へ反映」処理を復活させない。

### イベント通常在庫

**対象 `event_id` の `booth_event_items` が正式値。**

### ガチャ

**対象 `event_id` かつ `item_type = gacha_prize` の `booth_event_items` を使用。**

### 店舗共通イベント棚

`event_storage_stocks` は店舗共通イベント棚の現在値であり、特定イベントの現在在庫ではない。

### 履歴

`event_storage_movements`、`booth_stock_movements`、`inventory_logs`、販売履歴、`event_close_snapshot` 等は履歴 / 監査用途。movementのSUMや過去イベントを現在イベント在庫として使用しない。

## 7. イベント終了処理

- 戻り実数 → 通常棚へ戻す
- 売れた数量 → 戻さない
- 商品転用 / 消費数量 → 戻さない
- 未戻り → 勝手に戻さない

特定イベント終了時に他イベント由来の共通棚数量を減算してはいけない。対象 `event_id` に属するデータだけを処理する。

重要な既存修正：

- `booth_event_items.event_storage_qty` の対象イベント分だけを扱う
- `event_storage_stocks.storage_qty` 全体を特定イベント分として誤減算しない
- 締め解除後の削除で通常棚を二重復元しない
- 不正な `event_close_snapshot` を現在値計算に使わない

複数イベントで同一商品を使用している場合、イベントA終了でイベントBの数量が変化してはいけない。

## 8. 棚番

- 1商品 = 1現在棚番
- 最後に登録した棚番が現在棚番
- 過去の棚移動履歴は保持
- 棚番変更で在庫数量を変更しない
- 既存棚番商品の変更を可能にする

## 9. 商品転用の確認状態

SSOT：

- `inventory_logs.equipment_checked`
- `inventory_logs.equipment_checked_by`
- `inventory_logs.equipment_checked_at`

DBが確認済みならPC / モバイルとも「確認済み」と表示し、確認ボタンを出さない。確認操作は監査上の確認であり、在庫変更を再実行しない。必要な場面ではDB最新状態を再取得する。

## 10. スマレジ連携

スマレジは参照だけでなく実データへの書き込みを行う。

少なくとも以下を「外部システムを書き換える処理」として扱う：

- スマレジ在庫調整 / 在庫変更
- スマレジ売上登録
- スマレジ売上取消
- イベント / ガチャ等に関連するスマレジ在庫変更

エンドポイント、payload、API仕様は推測せず、既存 `api/` とスマレジ仕様を確認する。

### 書き込み時の注意

- GET/参照と書き込みを混同しない。
- Production書き込み前に対象・数量を記録する。
- 二重クリック、再送、timeout後再試行で二重売上・二重在庫調整を起こさない。
- ARICO成功 / スマレジ失敗、または逆の部分成功を考慮する。
- APIレスポンス確認前に成功表示しない。
- 失敗時にローカル状態だけ成功扱いで進めない。
- 売上登録 / 取消の対象取引を識別可能にする。
- 実売上に影響するテストを無断で行わない。

### オンライン注文の在庫変動

概ね以下の流れを考慮する：

- 受注：`取置き -N`
- ARICOピック：`出荷 N`
- オンライン出荷：`取置き +N` → `売上 -N`

差異分析で単一ログだけを見て異常と断定しない。

## 11. イベント販売取込とレジ設定

イベント販売用レジ設定の正式保存先は `event_register_settings`。

東京 / 愛知について、レジ名、`register_id`、`terminal_id` を扱う。

ID未設定時、表示用「未設定」「後で登録」等をDB値として保存しない。未設定は現行DB仕様に従いNULL等を使用する。

販売取込側も `event_register_settings` をSSOTとし、localStorage、JS定数、古いハードコードを正式値として使わない。ID未設定時はスマレジ販売APIを呼ばず安全に停止する。

販売取込では：

- 未持ち出し商品をAPI取得段階で捨てず明示する。
- API sort/orderを推測しない。
- barcodeしかない販売明細も安全にマスター照合できる場合は正規化する。
- 現在イベントとの紐付けがない商品を通常イベント販売として勝手に登録しない。

## 12. Supabase / SQL

最終報告には必ず以下を記載する：

- SQL変更：あり / なし
- RPC変更：あり / なし

### SQL適用方法

自動migrationの正式フローが明示されていない変更は、**Supabase SQL Editorで手動実行する前提**とする。エージェントが勝手に適用済みとみなさない。

SQLを作成した場合：

1. 目的を説明
2. 対象テーブル / RPCを明記
3. 既存データへの影響確認
4. rollback / 再実行安全性確認
5. Supabase SQL Editorへそのまま貼れるSQLを提示
6. 実行後に実DBスキーマ / RPCを再確認
7. Production UIから実操作テスト

無条件 `DELETE`、全件UPDATE、破壊的DROP等を安易に実行しない。

コード上の想像ではなくProduction実スキーマを正とする。過去に `inventory_count_items` で存在しない `before_stock`、`updated_at`、`reflected_at`、`reflected_by` を前提にした問題があった。新規カラム参照前に実在確認する。

## 13. RPC / Transaction

複数テーブルを一体変更する処理では部分成功を避ける。

特にイベント持ち出し、イベント終了、barcode変更、在庫変更+履歴登録等では原子性を検討する。DB内複数更新は必要に応じSupabase RPC / PostgreSQL transactionを使用する。

途中FAILで「現在値だけ変更」「履歴だけ登録」「一部商品だけ変更」を残さない。

## 14. バージョン管理

このリポジトリには複数のバージョン表現がある。

例：

- README等：`v90`
- Production / commit報告：`2.93.112`

同じ意味だと推測して勝手に変換しない。

### Productionアプリバージョン

現行報告は `2.93.xxx` 形式。既存の `version.js` とHTML側asset version/cache busting方式を確認し、既存方式に従う。

修正後は `version.js`、HTMLのJS参照、Production実配信assetが一致することを確認する。

### READMEの `v90` 等

用途をREADME / Git履歴で確認してから変更する。`2.93.112` を見てREADMEを自動的に `v112` 等へ変えない。体系不明なら既存値を維持する。

## 15. Git / Commit

変更前にHEADと未commit差分を確認し、ユーザーの既存変更を消さない。

Production修正の最終報告に以下を記載：

- Commit hash
- Production Ver
- push済みか
- Production反映確認

## 16. テスト方針

「構文チェックPASS」だけで完了にしない。

最低限：

1. コードチェック
2. Production配信確認
3. Production UI実操作
4. DB確認
5. 履歴確認
6. 再読込
7. 関連機能回帰

FAILは終了理由ではない。安全に修正可能なら、原因特定→修正→テスト→commit→push→Production→同一ケース再テスト→関連回帰まで進める。

一方、「完全PASS」という抽象目標で無限に監査を拡大しない。明示された業務フローと終了条件をPASSしたら、非阻害項目は残課題として分離する。

## 17. 主要回帰

### 在庫

商品検索、在庫変動、アプリ内棚卸、0棚卸、同数量棚卸、連続棚卸、棚番、商品別履歴、全体履歴、商品転用、スマレジ差異。

### イベント

新規イベント0商品/0数量、持ち出し、一覧、CSV、ガチャ、販売取込、戻り、終了、二重終了拒否、締め解除、削除/アーカイブ、別event_idへの影響0。

### 設定

商品マスター取込、barcode変更、barcode衝突、イベントレジ設定保存、再読込。

### Console / Network

`ReferenceError`、`TypeError`、Unhandled Promise、JS 404、API 500、Supabase重大エラー、Production asset不一致を0にする。

## 18. イベント回帰は動的に行う

特定商品・特定数量を永続的な期待値としてハードコードしない。

1. 現在アクティブな `event_id` を取得
2. その `event_id` の `booth_event_items` を取得
3. その値を期待値にする
4. イベント管理
5. 持ち出し在庫一覧
6. 棚卸分析
7. スマレジ差異

を突合する。

終了済みイベントの過去持ち出し数量を現在イベント在庫として表示しない。

## 19. UI / モバイル

PCだけでPASSにしない。

- モバイル幅で重要情報を消さない。
- 長い商品名・barcodeで崩さない。
- 横スクロール前提にしない。
- ボタンを不必要に巨大化しない。
- PCとスマホで業務上の状態表示を変えない。

過去に `equipmentCheckHtml()` がモバイル時に空文字を返し、商品転用の確認状態が消える不具合があった。レスポンシブ対応のために重要な業務状態を消さない。

## 20. 作業報告

修正ファイル一覧は階層が分かる形式で記載する。

必須報告：

- 原因
- 変更内容
- 影響範囲
- 修正ファイル一覧
- SQL有無
- RPC有無
- Git commit
- Production Ver
- Production反映
- Production実操作結果
- DB突合
- テストデータ復元
- リリース阻害バグ
- 残課題

テスト方法はユーザーがそのまま操作できる番号付き手順で記載する。「確認してください」だけで終わらせない。

## 21. エージェント行動原則

- 既存仕様を調べず作り直さない。
- Production実スキーマを正とする。
- 現在値と履歴を混同しない。
- event_idを跨いで在庫を混ぜない。
- barcode変更を軽視しない。
- 外部API書き込みを通常UI変更と同じ感覚で扱わない。
- エラーを隠してPASSにしない。
- UIだけ直してDB不整合を隠さない。
- DBだけ正しくしてUI不整合を放置しない。
- テストデータを残さない。
- 不要な許可待ちを挟まない。
- 認証・MFA・権限・危険な本番操作で本人判断が必要なら停止する。
- 無関係な大規模リファクタをしない。
- 新機能より既存業務の安全性を優先する。

## 22. 現在の基準

このファイル作成時点の直近Production：

- Production Ver：`2.93.113`
- Commit：`a28e518`

この値は将来更新されるため、作業開始時にGit HEAD / Production Verを必ず確認し、この記載だけを現在値として信用しない。

直近の重要修正：

- イベント終了時の共通イベント棚誤減算防止
- 締め解除後の削除による通常棚二重復元防止
- 不正な `event_close_snapshot` 登録停止
- 商品転用確認状態のPC / モバイル表示統一
- 持ち出し在庫一覧の削除済み関数参照修正
- イベント在庫の `event_id` 分離

これらを将来の修正で戻さない。
